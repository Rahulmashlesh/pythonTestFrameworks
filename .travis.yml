## Warning this is just an example file, use this as inspiration not implementation

language: python
python:
  - "3.7"
install:
  - pip install -r requirements.txt
script:
  - python -m unittest discover